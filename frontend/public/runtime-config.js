/*
 * Cấu hình runtime. Có thể sửa file này trực tiếp trên hosting sau khi build.
 * Để chuỗi rỗng thì frontend sẽ dùng REACT_APP_API_URL trong file .env.
 */
window.__GEARXIN_CONFIG__ = {
  API_URL: "",
  STORE_URL: "",
  REQUEST_TIMEOUT_MS: 20000,
};
